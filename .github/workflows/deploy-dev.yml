name: GitHub Pages development deployment

on:
  push:
    branches:
      - main

jobs:
  deploy-dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: 12.x

      - name: Cache node modules
        id: cache-node-modules
        uses: actions/cache@v2
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('package-lock.json') }}

      - name: Setup SSH to install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        uses: webfactory/ssh-agent@v0.5.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: npm ci

      - name: Create production build
        env:
          REACT_APP_ENVIRONMENT: development
          REACT_APP_INFURA_PROJECT_ID_DEV: ${{ secrets.REACT_APP_INFURA_PROJECT_ID_DEV }}
          REACT_APP_DAO_REGISTRY_CONTRACT_ADDRESS: "0x83eA12d54D5DCF88FeD2fB80dcEa1336A8Ac8924"
          REACT_APP_MULTICALL_CONTRACT_ADDRESS: "0xE1306199D95a1Ee0Ef155A984B0a0B50F7B0c216"
          REACT_APP_SNAPSHOT_HUB_API_URL: https://snapshot-hub-erc712.dev.thelao.io
          REACT_APP_SNAPSHOT_SPACE: tribute
          REACT_APP_GRAPH_API_URL: https://api.thegraph.com/subgraphs/name/sophiacodes/molochv3-dev
        run: npm run build

      - name: Deploy to GitHub Pages site
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          commit_message: ${{ github.event.head_commit.message }}
          cname: molochv3.org
