/**
 * TYPES
 */

// @todo Add more properties as needed for Member Profile features
export type Member = {
  address: string;
  delegateKey: string;
  isDelegated: boolean;
  shares: string;
  loot: string;
};
